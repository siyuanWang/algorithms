<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       					http://www.springframework.org/schema/beans/spring-beans.xsd
    					http://www.springframework.org/schema/context
    					http://www.springframework.org/schema/context/spring-context.xsd">


    <context:component-scan base-package="com.wsy.*"/>
    <context:property-placeholder location="classpath*:config/*.properties"/>
    <context:property-placeholder location="classpath*:config/*.json"/>

    <bean id="dataSource" class="com.wsy.learn.spring.placeholder.DataSource">
        <property name="driveClass" value="${driveClass}"/>
        <property name="url" value="${url}"/>
        <property name="userName" value="${userName}"/>
        <property name="password" value="${password}"/>
    </bean>

    <bean id="JSONUtil" class="com.wsy.learn.spring.placeholder.JSONUtil">
        <property name="in" value="classpath:template"/>
    </bean>
</beans>
